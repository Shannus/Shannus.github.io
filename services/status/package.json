{
  "name": "@portfolio/status",
  "version": "1.0.0",
  "private": true,
  "description": "System status service (V2 product line): API Gateway -> Lambda (no DB)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node esbuild.mjs",
    "package": "cd dist && zip -qr ../function.zip index.js && echo packaged function.zip",
    "audit": "npm audit --audit-level=high"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.145",
    "@types/node": "22.9.0",
    "esbuild": "0.24.0",
    "typescript": "5.6.3",
    "vitest": "2.1.4"
  }
}
